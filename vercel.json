{
  "version": 2,
  "builds": [
    {
      "src": "Server/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "Server/index.ts"
    }
  ],
  "buildCommand": "cd Server && npm install && cd ../Client && npm install && npm run build"
}